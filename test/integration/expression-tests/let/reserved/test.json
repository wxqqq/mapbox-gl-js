{
  "expectExpressionType": null,
  "expression": ["let", "zoom", 1, ["zoom"]],
  "expected": {
    "compiled": {
      "result": "error",
      "errors": [
        {
          "key": "[1]",
          "error": "\"zoom\" is reserved, so it cannot not be used as a \"let\" binding."
        }
      ]
    }
  }
}
